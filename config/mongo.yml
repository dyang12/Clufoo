defaults: &defaults
  host: 127.0.0.1
  port: 27017

development:
  <<: *defaults
  database: wufoo_clone_development

test:
  <<: *defaults
  database: wufoo_clone_test

# set these environment variables on your prod server
production:
  <<: *defaults
  database: wufoo_clone
  username: <%= ENV['MONGO_USERNAME'] %>
  password: <%= ENV['MONGO_PASSWORD'] %>
